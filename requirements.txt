# Dependencias existentes (ya usadas en claude_individual.py)
requests>=2.31.0
urllib3>=2.0.0

# V4.0: Scraping de polymarketanalytics.com
undetected-chromedriver>=3.5.0
selenium>=4.0.0
setuptools  # Requerido por undetected-chromedriver

# Para instalar el navegador Chrome:
# pip install playwright && playwright install chromium
# También requiere xvfb: sudo apt install xvfb

# Dependencias opcionales (no usadas en V4 pero útiles)
beautifulsoup4>=4.12.0
lxml>=4.9.0
